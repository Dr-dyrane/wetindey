export const copy = {
  accessibility: {
    changeLocation: (location: string) => `Change location, currently ${location}`,
    switchLightMode: "Switch to light mode",
    switchDarkMode: "Switch to dark mode",
    dismiss: "Dismiss"
  }
} as const;
